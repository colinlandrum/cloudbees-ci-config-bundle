removeStrategy:
  rbac: SYNC
  items: NONE
items:
- kind: folder
  name: colinlandrum
  groups:
  - members:
      users:
      - landrumlabs
      - landrumlabs-admin
    roles:
    - name: browse
      grantedAt: current
    - name: workshop-admin
      grantedAt: child
    name: Team Administrators
  filteredRoles:
  - workshop-admin
  - browse
  items:
  - kind: managedController
    name: controller
    properties:
    - healthReporting:
        enabled: true
    - configurationAsCode:
        bundle: colinlandrum-controller
    configuration:
      kubernetes:
        memory: 4000
        cpus: 1.0
        clusterEndpointId: default
        disk: 10
        storageClassName: premium-rwo
        domain: colinlandrum-controller
        namespace: controllers
        yaml: |
          kind: "StatefulSet"
          spec:
            template:
              metadata:
                labels:
                  networking/allow-internet-access: "true"
              spec:
                containers:
                - name: "jenkins"
                  env:
                  - name: "SECRETS"
                    value: "/var/jenkins_home/jcasc_secrets"
                  - name: "CONTROLLER_NAME"
                    value: "controller"
                  - name: "GITHUB_ORGANIZATION"
                    value: "colinlandrum"
                  - name: "GITHUB_USER"
                    value: "landrumlabs"
                  - name: "GITHUB_APP"
                    value: "cloudbees-ci-pipeline-workshop"
                  - name: "GITHUB_APP_ID"
                    value: ""
                  - name: "CONTROLLER_SUBDOMAIN"
                    value: "colinlandrum-controller"
                  - name: "CASC_BUNDLE_ID"
                    value: "colinlandrum-controller"
                  - name: "AVAILABILITY_PATTERN"
                    value: "cloudbees-ci-pipeline-workshop/colinlandrum/controller"
                  volumeMounts:
                  - name: "jcasc-secrets"
                    mountPath: "/var/jenkins_home/jcasc_secrets"
                volumes:
                - name: "jcasc-secrets"
                  secret:
                    secretName: cbci-mc-secret
